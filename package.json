{
  "name": "webalk_2026_beadando_travel_planner",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently -n backend,frontend -c yellow,cyan --kill-others-on-fail \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "postinstall": "npm install --prefix backend && npm install --prefix frontend",
    "db:migrate": "npm run prisma:migrate --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
